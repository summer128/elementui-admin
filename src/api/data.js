// 每个模块都应该有自己的接口文件去统一管理api
// import {get, post } from '@/api/axios'

// export const getDzData = (params) => get('poetry?count=2&page=1')

let httpUrl = {
    'news':'http://poetry.apiopen.top'
}

export {httpUrl}